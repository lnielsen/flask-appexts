============
Installation
============

At the command line::

    $ pip install flask-appexts

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask-appexts
    $ pip install flask-appexts
